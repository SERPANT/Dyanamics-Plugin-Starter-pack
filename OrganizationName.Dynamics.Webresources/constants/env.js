const organizationUrl = 'https://organizationName.crm.dynamics.com/api/data/v9.1/';

const requestTypes = {
    get: 'GET'
};

const httpStatus = {
    success: 200,
    readyState: 4
};

const requestReadyState = {
    done: 4
};
