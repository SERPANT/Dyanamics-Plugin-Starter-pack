/**
 * To be executed on onSave.
 * 
 * Tasks: 
 * 1. Tasks performed when the record is saved.
 * 
 * @param {objet} exeContext: provided by dynamics.
 */
function onSave(exeContext) {
    var formContext = exeContext.getFormContext();

    // List of function performing specific tasks when record is saved.
}

/**
 * Executes when the page first loads.
 * 
 * @param {object} executionContext : context object provided by dynamics.
 */
function onLoad(executionContext) {
    var formContext = executionContext.getFormContext();

    // List of function performing specific tasks when record is saved.
}
